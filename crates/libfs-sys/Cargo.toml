[package]
name = "libfs-sys"
version.workspace = true
edition.workspace = true
description = "Low-level FFI bindings for libfs (FreeSWITCH) RTP and audio APIs"
build = "build.rs"

[dependencies]
libc.workspace = true

[build-dependencies]
# Manual FFI bindings - no bindgen
